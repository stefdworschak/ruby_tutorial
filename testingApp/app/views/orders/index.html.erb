<h1>Listing orders</h1>
<table>
<thead>
<tr>
<th>Product</th>
<th>Details</th>
<th>Customer</th>
<th></th>
<th></th>
<th></th>
</tr>
</thead>
<tbody>
<% @orders.each do |order| %>
<tr>
7
<td><%= order.product %></td>
<td><%= order.details %></td>
<td><%= order.customer_id %></td>
<td><%= link_to 'Show', customer_order_path(@customer, order) %>
</td>
<td><%= link_to 'Edit', edit_customer_order_path(
@customer, order) %>
</td>
<td><%= link_to 'Destroy', customer_order_path(
@customer, order), method: :delete,
data: { confirm: 'Are you sure?' } %>
</td>
</tr>
<% end %>
</tbody>
</table>
<br>
<%= link_to 'New order', new_customer_order_path(@customer) %>
<%= link_to 'Back to Customer', @customer %>